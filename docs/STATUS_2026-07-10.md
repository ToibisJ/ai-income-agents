# סטטוס סוף יום — 2026-07-10

איפה עצרנו בפרויקט אתר קורות החיים (repo: `ToibisJ/50plus`, אתר חי: `50plus-xi.vercel.app`).

## מה חי ועובד עכשיו
- **דף נחיתה** (`index.html`) — הוק, תמחור (חינם/₪180/₪290/₪420 עם דרגות קידום), מעסיקים, תנאי שימוש, רספונסיבי.
- **טופס העלאה** (`upload.html`) — שדות חובה: תפקיד, חברה, מה עשית, הישגים; אופציונלי: הדבקת טקסט/העלאת PDF-תמונה; צ'ק-בוקס הסכמה לתנאים (חובה) + הצגה למעסיקים (מסומן כברירת מחדל). מחובר ל-Supabase דרך fetch ישיר.
- **חיפוש** (`search.html`) — מציג את כל המועמדים, חיפוש מילת מפתח על כל השדות, מיון לפי דרגת קידום, צ'יפים שנבנים אוטומטית מהמקצועות שבמאגר (כל מקצוע חדש מופיע מעצמו).
- **תנאי שימוש** (`terms.html`) — אין הבטחת עבודה/התאמה, אין התחייבות לזמינות, שירות = כתיבה בלבד.

## תשתית
- **Supabase** מחובר (פרויקט `fiftyplustalent`, URL `https://zcsowbdulzzhxcssgmgd.supabase.co`). הורץ: טבלת `candidates`, view `public_candidates`, policy `anon_upload_cvs` על bucket `cvs`. Publishable key בקוד (בטוח, RLS מגן).
- **בלי SDK חיצוני** — הכל fetch ישיר ל-REST/Storage, עמיד בפני חוסמי פרסומות (זה מה שתקע קודם — ה-CDN של supabase-js נחסם ע"י AdBlock).
- **Vercel**: Framework=Other, Root=`/`. האפליקציה הישנה בארכיון תחת `old-stuff/`.
- **וואטסאפ**: 054-478-2707 (מספר "טויביס מפעל אתרים" הישן, לשנות שם פרופיל ל"טויביס פתרונות תוכנה").

## נתוני בדיקה
200 מועמדים במאגר, 20 מקצועות, מייל שונה לכל מקצוע (`*.test@example.com`), שנות ניסיון שונות בכל רשומה.

## נשאר לפני קהל אמיתי (TODO)
1. **למחוק את נתוני הבדיקה:** `delete from public.candidates where contact_email like '%test%@example.com';` — חובה, אחרת זו הטעיית נפח מזויף.
2. **להחזיר את סמל הנגישות** — הוסתר לבקשת יונתן (`.a11y-btn,.a11y-panel{display:none}` ב-4 הדפים); דרישה חוקית באתר ציבורי בישראל.
3. **לבדוק טופס העלאה מקצה-לקצה** — להעלות קו"ח אמיתי ולוודא שנכנס למסד ומופיע בחיפוש.
4. פתוח לעתיד (כשיהיו ~500 אנשים): לצמצם צ'יפים ל"נפוצים", ורעיון ₪1 למעסיק לכל פנייה.

## הקשר
המשך של [[SPEC_cv_service_v1]] ו-[[ROLES]]. יונתn = מנהל פרויקט, George = מו"פ. עצרנו בערב 10.7, אמור להמשיך בערב/למחרת.
